# Set one of these to enable real LLM calls.
# Without a key, Drift uses a mock provider that returns canned responses.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Optional — only needed if you wire up `memory: dendric(...)` blocks
# DATABASE_URL=postgresql://postgres:postgres@localhost:5433/dendric
